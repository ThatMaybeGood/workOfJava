<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reports.mapper.QcMaintainMapper">

    <!-- 按月份查询人工维护记录（人工登记优先，同来源取最新） -->
    <select id="queryByMonth" resultType="com.reports.entity.QcMaintainEntity">
        SELECT
            id AS id,
            stat_month AS statMonth,
            indicator_code AS indicatorCode,
            indicator_name AS indicatorName,
            numerator AS numerator,
            denominator AS denominator,
            rate AS rate,
            source_type AS sourceType,
            create_time AS createTime,
            update_time AS updateTime,
            ext1 AS ext1,
            ext2 AS ext2,
            ext3 AS ext3
        FROM TR_QC_MAINTAIN
        WHERE stat_month = #{statMonth}
        ORDER BY indicator_code, DECODE(source_type, '人工登记', 0, 1), update_time DESC
    </select>

    <!-- 按月份区间查询（报表取数用；人工登记优先，同来源取最新） -->
    <select id="queryByRange" resultType="com.reports.entity.QcMaintainEntity">
        SELECT
            id AS id,
            stat_month AS statMonth,
            indicator_code AS indicatorCode,
            indicator_name AS indicatorName,
            numerator AS numerator,
            denominator AS denominator,
            rate AS rate,
            source_type AS sourceType,
            create_time AS createTime,
            update_time AS updateTime,
            ext1 AS ext1,
            ext2 AS ext2,
            ext3 AS ext3
        FROM TR_QC_MAINTAIN
        WHERE stat_month BETWEEN #{startMonth} AND #{endMonth}
        ORDER BY stat_month, indicator_code,
                 DECODE(source_type, '人工登记', 0, 1), update_time DESC
    </select>

    <!-- 新增维护记录（主键取序列） -->
    <insert id="insert" parameterType="com.reports.entity.QcMaintainEntity">
        INSERT INTO TR_QC_MAINTAIN (
            id, stat_month, indicator_code, indicator_name,
            numerator, denominator, rate, source_type,
            create_time, update_time, ext1, ext2, ext3
        ) VALUES (
            seq_tr_reports.NEXTVAL, #{statMonth}, #{indicatorCode}, #{indicatorName},
            #{numerator}, #{denominator}, #{rate}, NVL(#{sourceType}, '人工登记'),
            SYSDATE, SYSDATE, #{ext1}, #{ext2}, #{ext3}
        )
    </insert>

</mapper>
