<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mergedata.mapper.CashMapper">

    <resultMap id="CashMap" type="com.mergedata.model.entity.YQCashRegRecordEntity">
        <id column="operator" property="operator"/>
        <resultDTO column="operator_no" property="operatorNo"/>
        <resultDTO column="amount" property="retainedCash"/>
        <resultDTO column="apply_date" property="applyDate"/>
        <resultDTO column="create_time" property="createTime"/>
        <resultDTO column="ope_type" property="opeType"/>
        <resultDTO column="window_no" property="windowNo"/>
        <resultDTO column="scheduling" property=" scheduling"/>
        <resultDTO column="save_date" property="saveDate"/>

    </resultMap>


    <select id="selectByDate" resultMap="CashMap">
        select
        operator,
        operator_no,
        amount,
        apply_date ,
        create_time,
        ope_type,
        window_no,
        scheduling,
        save_date
        from
        mpp_cash_reg_record
        WHERE save_date= #{date}
    </select>
</mapper>