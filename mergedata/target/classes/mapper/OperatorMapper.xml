<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mergedata.mapper.OperatorMapper">

    <resultMap id="YQOperatorMap" type="com.mergedata.model.YQOperator">
        <id column="operator_no" property="operatorNo"/>
        <result column="operator_name" property="operatorName"/>
        <result column="department_id" property="departmentId"/>
        <result column="isvalid" property="isValid"/>
        <result column="creator" property="creator"/>
        <result column="create_time" property="createTime"/>
        <result column=" updator" property=" updator"/>
        <result column="update_time" property="updateTime"/>
        <result column="update_count" property="updateCount"/>
    </resultMap>

    <select id="selectAll" resultMap="YQOperatorMap">
        SELECT
        operator_no,
        operator_name,
        department_id,
        isvalid,
        creator,
        create_time,
         updator,
        update_time,
        update_count
        FROM
        MPP_CASH_REG_OPERATOR
        WHERE
        isvalid = '1'
    </select>

    <select id="selectByID" resultMap="YQOperatorMap">
        SELECT
        operator_no, operator_name, department_id, isvalid, creator,
        create_time,  updator, update_time, update_count
        FROM
        MPP_CASH_REG_OPERATOR
        WHERE
        operator_no = #{operatorNo}
        AND isvalid = '1'
    </select>

    <insert id="insert" parameterType="com.mergedata.model.YQOperator">
        INSERT INTO MPP_CASH_REG_OPERATOR (
        operator_no, operator_name, department_id, isvalid, creator,
        create_time,  updator, update_time, update_count
        )
        VALUES (
        #{operatorNo}, #{operatorName}, #{departmentId}, #{isValid}, #{creator},
        #{createTime}, #{ updator}, #{updateTime}, #{updateCount}
        )
    </insert>

    <insert id="batchInsertList">
        INSERT INTO MPP_CASH_REG_OPERATOR (
        operator_no, operator_name, department_id, isvalid, creator,
        create_time,  updator, update_time, update_count
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.operatorNo}, #{item.operatorName}, #{item.departmentId}, #{item.isValid}, #{item.creator},
            #{item.createTime}, #{item. updator}, #{item.updateTime}, #{item.updateCount}
            )
        </foreach>
    </insert>

</mapper>